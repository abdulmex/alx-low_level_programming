#include "main.h"

/**
 * mul - multiplies two given integers
 * @a: first int
 * @b: second int to multiply with
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
