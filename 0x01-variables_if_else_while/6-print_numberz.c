#include <stdio.h>

/**
 * main - prints numbers 1-10
 * none
 * Return: success value 0
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
		putchar(num);
	putchar(10);
	return (0);
}
